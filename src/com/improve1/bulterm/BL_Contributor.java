package com.improve1.bulterm;

//import java.sql.ResultSet;
//import java.sql.SQLException;

public class BL_Contributor {
/*
	private TermNameList getTermNames(ResultSet rset) {
		TermNameList termNames = new TermNameList();
		TermName termName = null;
		try {
			termName = new TermName();
			termName.setLanguageId(1);
			if (rset.getString(2) == null) {
				termName.setTermName("");
			} else {
				termName.setTermName(rset.getString(2));
			}
			termNames.add(termName);
			termName = null;
			// termObj.setTermName();
			termName = new TermName();
			termName.setLanguageId(2);
			if (rset.getString(3) == null) {
				termName.setTermName("");
			} else {
				termName.setTermName(rset.getString(3));
			}
			termNames.add(termName);
			termName = null;
			termName = new TermName();
			termName.setLanguageId(3);
			if (rset.getString(4) == null) {
				termName.setTermName("");
			} else {
				termName.setTermName(rset.getString(4));
			}
			termNames.add(termName);
			termName = null;
			termName = new TermName();
			termName.setLanguageId(4);
			if (rset.getString(5) == null) {
				termName.setTermName("");
			} else {
				termName.setTermName(rset.getString(5));
			}
			termNames.add(termName);
			termName = null;
			termName = new TermName();
			termName.setLanguageId(5);
			if (rset.getString(6) == null) {
				termName.setTermName("");
			} else {
				termName.setTermName(rset.getString(6));
			}
			termNames.add(termName);
			termName = null;
		} catch (SQLException e) {
			e.printStackTrace();
		}
		return termNames;
	}//getTermNames()
	
	/*private String getColumnName(String columnToSearch) {
		String columnName = null;
		// if (columnToSearch == "groupIdentifier") {
		// columnName = "groupIdent";
		// } else if (columnToSearch == "groupName") {
		// columnName = "groupName";
		// } else if (columnToSearch == "standardIdentifier") {
		// columnName = "standardIdent";
		// } else if (columnToSearch == "standardName") {
		// columnName = "standardName";
		// } else if (columnToSearch == "termName") {
		// columnName = "em.name_a"; // termName
		// } else if (columnToSearch == "termDescription") {
		// columnName = "termDescription";
		// }
		columnName = columnToSearch;
		return columnName;
	}*/
}//class
